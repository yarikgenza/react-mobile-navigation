import React from 'react';

const propTypes = {};

const defaultProps = {};

export const ActionSheetListComponent = () => (
  <div>
    {'ActionSheetList Options'}
  </div>
);

ActionSheetListComponent.propTypes = propTypes;
ActionSheetListComponent.defaultProps = defaultProps;
